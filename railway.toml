[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python main.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5

# No healthcheck — this is a worker, not a web server.
# Railway will restart on non-zero exit code.
